Place the current CV at `static/files/bar-jacobi-cv.pdf`.

The Hugo header checks for that exact file. When present, the CV navigation item links directly to the PDF using Hugo's URL helpers; otherwise it links to the visible CV placeholder page.
